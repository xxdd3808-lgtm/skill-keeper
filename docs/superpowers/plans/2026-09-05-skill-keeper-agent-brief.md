# 给执行 Agent 的任务书

你是 skill-keeper 优化的执行者。先读本仓库 AGENTS.md、docs/superpowers/specs/2026-09-05-skill-keeper-review.md 和 docs/superpowers/plans/2026-09-05-skill-keeper-optimization.md；后两份是本次工作来源。断点续跑先读 PROGRESS.md/BLOCKED.md，每完成一项立即更新。拿不准的写 BLOCKED，继续不依赖它的部分，不能靠猜测放宽安全规则。

这活为什么干：把现有“盘点可信、建议有效、变更可恢复”的承诺真正贯通，减少后续反复补漏洞和人工判断的成本。取舍顺序：资产安全与可恢复 > 事实准确 > 用户操作闭环 > 性能。计划中的“必须/不得”是验收条件，“建议”可用更好方案替代并记录理由。

## 我替领导拍的板

- 默认（未逐项征询）：保留个人本地工具、Python 3.8+ 标准库、可选 PyYAML/gh，不整体重写，不引入数据库、模型 API、常驻服务或更多客户端；代价是新功能延后。
- 默认：串行按阶段 A 安全、B 可信工作流、C 效率交付；不要求多 Agent。
- 默认：本任务实现和测试代码，不替用户删除/更新/恢复真实 Skill，也不自动迁移真实数据。数据迁移只提供预演。

## 界限

仅修改详细计划白名单内的 scripts、tests、虚构 examples、相关 docs/README/SKILL/AGENTS、.gitignore、启动器和进度文件。真实客户端目录、插件缓存、个人 data/backups 只读。新回归只用临时 HOME；不得读取/输出 token/key/cookie/env。保留当前用户修改及本地提交，不 reset 到 origin，不自动 push/发布/改 Git 历史。

## 现状与任务 0

2026-09-05 实测基线 f044e1732e1fa1f620a2bed641e1542fb553b047：42 条历史提交，27 个 Python 实现文件、5,053 行；Python 3.9.6，126 项 unittest 全过、0 skipped、5.385 秒。本地端口测试需要允许 127.0.0.1 绑定。审视发现通过备份校验仍无法还原内部链接/目录权限、恢复 manifest 路径越界、删除验证异常不回滚、计划可重放、非法 vet 值放行及界面状态陈旧等可复现问题。

先跑 git status --short、git rev-parse HEAD、python3 --version、python3 -m unittest discover -s tests。数字不同先记录差异、保护已有修改，建立当前真实基线；环境问题记 BLOCKED，不跳过。核验后写≤10行开工回执再实现。

## 任务

严格按详细 Plan 的 Task 1–9 和文件/接口合同实施：备份/恢复合同→执行时策略→事务/中断恢复→观察完整性→审查历史与失效→完整候选与缓存→CLI/API/报告闭环→去重计算/外部运行态→验收入口/文档。

每项先复现红测试，再修实现，运行相关测试和全量回归，更新文档/进度，小提交。阶段 A 未过不得开放新增写入口。中断、部分 I/O 失败、错误状态和并发必须用临时 fixture 反向验证；确认过的计划与真实执行对象必须一致。

性能依据：80 个虚构 Skill 原来读正文12,800次、打分259,120次。优化目标读≤80次、打分≤3,160次，并与冻结的候选结果等价，不能删候选、改权重或降低安检完整性来达标。

## 规矩

禁止删测试、skip、放宽断言、mock 掉被测业务函数、把错误吞成成功、用 || true 造绿灯。现有126项风险语义不退；新必填字段可补fixture，本计划明确纠正的旧错误预期须记录F编号。故障注入只替换I/O/网络/验证边界。单条验收连败3次换独立任务并记BLOCKED，不能跳过阶段门槛宣称完成；结果变差撤销自己的失败尝试，保留用户改动。

## 完成条件

1. F01–F08 的失败用例全部转为安全结果，三种操作从CLI/API到磁盘/审计/报告贯通；审查历史与失效一致；F09次数和结果等价达标；F10隔离及迁移预演、F11文档与验收完成。
2. 原有测试风险覆盖不退、0 skipped，详细验收矩阵全过；真实Skill/插件缓存/个人数据未改，无个人数据入Git、无擅自远端操作。

交付必须贴实际测试摘要、关键红→绿、中断恢复、性能前后计数和Git范围；BLOCKED无问题也写“无”。未验证内容如实列出，不能只说“已完成”。
