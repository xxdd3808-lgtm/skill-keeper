# 安全策略

本文说明 skill-keeper 的威胁模型、真实变更边界、数据留存与漏洞报告方式。

## 1. 威胁模型:位置声明是不可信输入

大模型(或任何调用方)通过 `--root CLIENT=PATH`、`--locations-json FILE|-` 提交的
"位置声明"只用于告知本机 Skill 根目录,**一律视为不可信输入**:

- 字段白名单:仅 `schema_version` / `client` / `observed_by` / `complete` /
  `roots[path|scope|load_state]`;`mutable`、`instance_id`、`tree_hash`、命令、
  网址、秘密字段等任何白名单外内容整体拒绝(解析错误不回显字段值);
- 限额:总输入 ≤ 64 KiB、roots ≤ 32、字符串 ≤ 4 KiB、JSON 嵌套 ≤ 6 层、必须 UTF-8;
- `load_state` 只允许 `reported`:模型只能"客户端自报",永远不能自证 confirmed;
- 声明默认只读、仅用于本次扫描、不单独持久化、不上传;声明产生的实例
  **永远不可变**,在 policy 与 service 两层都没有删除/更新入口;
- 同一真实路径若已被本机适配器/本地配置登记,本机事实优先,声明副本丢弃;
- 解析阶段绝不打开声明中的任何文件;文件读取只发生在本机扫描器按已校验根目录进行。

## 2. 真实变更边界

- 扫描、报告、更新检查、审查队列只读;**系统永不自动删除**;
- 删除/更新/恢复必须:不可变计划 → 用户逐字确认 digest → 互斥锁 → 创建并验证备份
  → 持久事务状态 → 原子移动 → 失败自动回滚 → 审计(`data/audit-v2.jsonl`);
- apply 在取得锁后、移动目标前,于**每个目标同目录**用工具自有临时对象实际验证
  创建/同卷 rename/replace/fsync 并全部清理;失败在改动任何目标前中止;
- 变更目标只能是当前 inventory 里本机扫描、mutable 且策略允许的稳定 instance ID;
  路径、`.`、`..`、逃逸符号链接一律拒绝;归档成员路径严格保持 POSIX `/`,
  拒绝反斜杠、盘符组件、绝对路径与 `..`;
- 自建白名单、known-sources `builtin-app`(含 owner 语义)、客户端自带/插件内容受保护;
  保护配置损坏时拒绝一切写操作;
- 互斥锁跨进程生效(POSIX flock / Windows msvcrt);中断事务凭持久状态恢复,不猜目录名。

## 3. 数据留存

- 全部数据留在本机:盘点、报告、审查台账、备份、审计均在本地文件;
- 默认不联网、不上传、无遥测;唯一可选的网络功能是 GitHub 仓库证据/候选树拉取
  (缺席 gh CLI 或网络失败时明确降级并标注);
- 运行态目录:可识别旧仓库运行态沿用仓库 `data/`+`backups/`;新安装统一
  `~/.skill-keeper/{data,cache,backups}`;候选暂存在系统缓存目录;
- 不记录 Skill 正文、完整 HOME、客户端账号、提示词、token/key/cookie/env;
  客户端配置只按字段白名单读取,秘密字段既不返回也不进入错误信息。

## 4. 漏洞报告

- 请通过 GitHub 私有安全公告(Repository → Security → Report a vulnerability)
  私密报告,不要在公开 issue 中披露可利用细节;
- 报告请包含:影响范围(扫描/报告/变更/服务)、复现步骤或最小 PoC、涉及平台;
- 维护者会在确认后评估严重度并发布修复;修复发布前不公开细节。

## 5. 明确不提供

无服务端/多用户体系、无云端 Skill 管理、无客户端支持等级排行、无遥测文档。
本工具的安全边界是"单机、单用户、本地文件"。
